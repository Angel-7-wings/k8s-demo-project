apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "billings.fullname" . }}-test-connection"
  labels:
    {{- include "billings.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "billings.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
